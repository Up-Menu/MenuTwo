import React from 'react';
import { Typography } from '@mui/material';

const Finish = () => {
  return (
    <div>
      <Typography variant='h3'>
        شما نصب را به اتمام رساندید
      </Typography>
    </div>
  );
};

export default Finish;